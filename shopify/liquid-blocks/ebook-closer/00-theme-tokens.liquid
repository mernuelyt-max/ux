{% comment %}
  U DEL CLOSER — Design tokens (negro-rojo)
  Pegar como PRIMER bloque "Liquid personalizado" de la página, antes que
  cualquier otro bloque de este paquete. Solo define variables CSS globales,
  no renderiza nada visible.
{% endcomment %}
<style>
  :root{
    --udc-black:#060606;
    --udc-black-soft:#121212;
    --udc-surface:#1a1a1a;
    --udc-surface-2:#232323;
    --udc-red:#ff1b3d;
    --udc-red-dark:#b4001f;
    --udc-red-glow:rgba(255,27,61,.35);
    --udc-white:#f7f7f7;
    --udc-muted:#b8b8b8;
    --udc-border:#2c2c2c;
    --udc-radius:14px;
    --udc-font:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
    --udc-container:1120px;
  }
</style>
